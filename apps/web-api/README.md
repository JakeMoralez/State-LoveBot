# Wave 0 placeholder. Source of truth today:
#   sibling repo `State-LoveAdmin/backend` → `/opt/State-Love-Admin`
#
# Later waves copy FastAPI app here; routers only call `packages.application.*`.
# Bot will not use raw SQL against panel tables (architecture 2a / shared use-cases).
