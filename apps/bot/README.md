# Wave 0 placeholder. Production entry remains repository root `main.py`
# so existing systemd (`/opt/State-LoveBot/venv/bin/python main.py`) keeps working.
#
# Later waves move `modules/`, `services/`, `database/` under this package
# and thin handlers will call `packages.application.*` use-cases.
